def make_3D_plot():
    # matched_jobs['first_seen'] = pd.to_datetime(matched_jobs['first_seen'])
    # aggregated_df = matched_jobs.groupby(['first_seen', 'search_city']).size().reset_index(name='post_count')
    # plot_3d = px.scatter_3d(aggregated_df, x='first_seen', y='job_title', z='post_count', labels={'post_count': 'Number of Job Postings', 'first_seen': 'Date'})
    # plot_3d.update_traces(mode='lines+markers')
    # plot_3d.update_layout(hovermode='x unified')   
    return {}